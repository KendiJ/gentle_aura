import 'package:flutter/material.dart';

class JustHome extends StatelessWidget {
  const JustHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}